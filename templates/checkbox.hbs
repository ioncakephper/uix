  <div class="form-group form-check">
    <label class="form-check-label">
      <input class="form-check-input" type="checkbox" name="{{{name}}}" id="{{{name}}}"{{#if checked}} checked{{/if}}> {{{label}}}
    </label>
  </div>