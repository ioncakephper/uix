    <label for="{{{name}}}">{{{label}}}:{{#if required}}<span class="form-label-required">*</span>{{/if}}</label>
    <input type="datetime-local" class="form-control" name="{{{name}}}" id="{{{name}}}"{{#if value}} value="{{{value}}}"{{/if}} />